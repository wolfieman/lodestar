# Lean runtime for shared hosting (Namecheap cPanel "Setup Python App", LiteSpeed LSAPI).
#
# Native WSGI via Flask — no ASGI bridge (LSAPI hangs on a2wsgi-wrapped FastAPI). Skips the
# heavy semantic-RAG stack (lancedb/fastembed/onnxruntime), which is RAM/CPU/inode-hungry on
# a shared tier; the app degrades to BM25 keyword retrieval, ample for the small knowledge
# base. Use requirements.txt for the full hybrid stack on Docker / a host with more RAM.
anthropic
flask
rank-bm25
python-dotenv
